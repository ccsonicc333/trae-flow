{
  "name": "mineradio-bridge-bundle",
  "version": "1.3.1",
  "private": true,
  "description": "esbuild bundler for Mineradio Bridge JS (consumed by TRAE Flow JSContext)",
  "scripts": {
    "bundle": "esbuild bridge-entry.js --bundle --format=iife --outfile=../TraeFlow/Resources/MineradioBridge/bridge-bundle.js"
  },
  "devDependencies": {
    "esbuild": "^0.23.0"
  }
}
